#!/usr/bin/env bash
# /review-prep slash command wrapper
# Usage: /review-prep or ./review-prep

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/scripts/review_prep.sh" "$@"